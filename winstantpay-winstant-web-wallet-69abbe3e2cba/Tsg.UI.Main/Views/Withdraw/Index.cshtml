@using System.Text.RegularExpressions
@using Tsg.UI.Main.App_LocalResources
@using TSG.Models.ServiceModels.AutomaticExchange
@model Tsg.UI.Main.Models.NewInstantPaymentViewModel
@{
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
    ViewBag.Title = GlobalRes.WithdrawIndexPage_Title;
}
<style type="text/css">
    .scrollable-menu {
        height: auto;
        max-height: 200px;
        overflow-x: hidden;
    }

    .bs-searchbox {
        padding: 4px 8px;
    }

    .error_btn {
        border: 1px solid #b94a48;
    }

    .success_btn {
        border: 1px solid #3A3A3A;
    }
</style>
<script type="text/javascript" src="/Content/assets/vendor/jquery/jquery.js"></script>
<script type="text/javascript" src="/Content/assets/vendor/qrReader/qr_packed.js"></script>
@{
    var currentCurrency = UiHelper.PrepareAvailableCurrenciesByService().FirstOrDefault();
    string u = Request.ServerVariables["HTTP_USER_AGENT"];
    Regex b = new Regex(@"(android|bb\d+|meego).+mobile|avantgo|bada\/|blackberry|blazer|compal|elaine|fennec|hiptop|iemobile|ip(hone|od)|iris|kindle|lge |maemo|midp|mmp|mobile.+firefox|netfront|opera m(ob|in)i|palm( os)?|phone|p(ixi|re)\/|plucker|pocket|psp|series(4|6)0|symbian|treo|up\.(browser|link)|vodafone|wap|windows ce|xda|xiino", RegexOptions.IgnoreCase | RegexOptions.Multiline);
    Regex v = new Regex(@"1207|6310|6590|3gso|4thp|50[1-6]i|770s|802s|a wa|abac|ac(er|oo|s\-)|ai(ko|rn)|al(av|ca|co)|amoi|an(ex|ny|yw)|aptu|ar(ch|go)|as(te|us)|attw|au(di|\-m|r |s )|avan|be(ck|ll|nq)|bi(lb|rd)|bl(ac|az)|br(e|v)w|bumb|bw\-(n|u)|c55\/|capi|ccwa|cdm\-|cell|chtm|cldc|cmd\-|co(mp|nd)|craw|da(it|ll|ng)|dbte|dc\-s|devi|dica|dmob|do(c|p)o|ds(12|\-d)|el(49|ai)|em(l2|ul)|er(ic|k0)|esl8|ez([4-7]0|os|wa|ze)|fetc|fly(\-|_)|g1 u|g560|gene|gf\-5|g\-mo|go(\.w|od)|gr(ad|un)|haie|hcit|hd\-(m|p|t)|hei\-|hi(pt|ta)|hp( i|ip)|hs\-c|ht(c(\-| |_|a|g|p|s|t)|tp)|hu(aw|tc)|i\-(20|go|ma)|i230|iac( |\-|\/)|ibro|idea|ig01|ikom|im1k|inno|ipaq|iris|ja(t|v)a|jbro|jemu|jigs|kddi|keji|kgt( |\/)|klon|kpt |kwc\-|kyo(c|k)|le(no|xi)|lg( g|\/(k|l|u)|50|54|\-[a-w])|libw|lynx|m1\-w|m3ga|m50\/|ma(te|ui|xo)|mc(01|21|ca)|m\-cr|me(rc|ri)|mi(o8|oa|ts)|mmef|mo(01|02|bi|de|do|t(\-| |o|v)|zz)|mt(50|p1|v )|mwbp|mywa|n10[0-2]|n20[2-3]|n30(0|2)|n50(0|2|5)|n7(0(0|1)|10)|ne((c|m)\-|on|tf|wf|wg|wt)|nok(6|i)|nzph|o2im|op(ti|wv)|oran|owg1|p800|pan(a|d|t)|pdxg|pg(13|\-([1-8]|c))|phil|pire|pl(ay|uc)|pn\-2|po(ck|rt|se)|prox|psio|pt\-g|qa\-a|qc(07|12|21|32|60|\-[2-7]|i\-)|qtek|r380|r600|raks|rim9|ro(ve|zo)|s55\/|sa(ge|ma|mm|ms|ny|va)|sc(01|h\-|oo|p\-)|sdk\/|se(c(\-|0|1)|47|mc|nd|ri)|sgh\-|shar|sie(\-|m)|sk\-0|sl(45|id)|sm(al|ar|b3|it|t5)|so(ft|ny)|sp(01|h\-|v\-|v )|sy(01|mb)|t2(18|50)|t6(00|10|18)|ta(gt|lk)|tcl\-|tdg\-|tel(i|m)|tim\-|t\-mo|to(pl|sh)|ts(70|m\-|m3|m5)|tx\-9|up(\.b|g1|si)|utst|v400|v750|veri|vi(rg|te)|vk(40|5[0-3]|\-v)|vm40|voda|vulc|vx(52|53|60|61|70|80|81|83|85|98)|w3c(\-| )|webc|whit|wi(g |nc|nw)|wmlb|wonu|x700|yas\-|your|zeto|zte\-", RegexOptions.IgnoreCase | RegexOptions.Multiline);
    bool isTablet = v.IsMatch(u.Substring(0, 4));
    bool isMobile = b.IsMatch(u);
    
    var aliases = UiHelper.PrepareAccountAliases();
    
}
<form id="form" action="/Payment/Create" method="post" class="form-horizontal" @*novalidate="novalidate"*@>
    @Html.AntiForgeryToken()
    <section class="panel panel-featured panel-featured-info">
        <header class="panel-heading">
            <h2 class="panel-title" id="panel-title">@GlobalRes.WithdrawIndexPage_Subtitle</h2>
        </header>

        <div class="panel-body">
            <div class="validation-message" id="validation-message">
                <ul></ul>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">Tap to scan QR</label>
                <div class="col-sm-6">
                    <a id="btn-scan-qr">
                        <i class="fa fa-qrcode fa-3x"></i>
                    </a>
                </div>
            </div>

            <div class="form-group">
                <label class="col-sm-3 control-label"></label>
                <div class="col-sm-3">
                    <a id="btn-scan-qr-close" hidden="hidden">
                        Close
                    </a>
                    <canvas hidden="" id="qr-canvas"></canvas>
                </div>
            </div>
            <div id="merchant-details" hidden="hidden">
                <div class="form-group">
                    <label class="col-sm-3 control-label">Merchant WPayID</label>
                    <div class="col-sm-6">
                        <input class="form-control" id="merchantAlias" name="merchantAlias" required="" type="hidden" value="" />
                        <p id="merchant-wpay-id"></p>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label">Currency</label>
                    <div class="col-sm-6">
                        <input class="form-control" id="currencyCode" name="currencyCode" required="" type="hidden" value="" />
                        <p id="currency"></p>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label text-right">@GlobalRes.Shared_NewInstantPaymentFormPage_Amount <span class="required" aria-required="true">*</span></label>
                    <div class="col-sm-6">
                        <input class="form-control" id="amount" name="amount" required="" type="number" step="0.01" value="" />
                        <div id="amountError" style="color: red; display: none;"></div>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label" style="text-align: right">
                        @GlobalRes.QrGenPage_SelectAlias  <span class="required" aria-required="true">*</span>
                    </label>
                    <div class="col-sm-6">
                        @Html.DropDownList("ddlAlias", aliases, new { style = "width:100%", id = "ddlAlias", @class = "form-control ibc" })
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label" style="text-align: right">
                    </label>
                    <div class="col-sm-6">
                        <span class="error">@GlobalRes.WithdrawIndexPage_Request_Message</span>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label text-right"></label>
                    <div class="col-sm-6">
                        <button type="submit" class="btn btn-primary" name="submitButton" value="Request">Request</button>
                        <a href="/home/index" class="btn btn-secondary" name="cancelButton" value="Cancel">Cancel</a>
                    </div>
                </div>
            </div>
            <div id="withdraw-confirmation" hidden="hidden">
                <div class="form-group">
                    <label class="col-sm-3 control-label">Reference</label>
                    <div class="col-sm-6">
                        <input class="form-control" id="paymentId" name="paymentId" required="" type="hidden" value="" />
                        <input class="form-control" id="paymentReference" name="ReferenceReference" required="" type="hidden"value="" />
                        <p id="payment-reference"></p>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label">Member WPayID</label>
                    <div class="col-sm-6">
                        <input class="form-control" id="memberWPayId" name="memberWPayId" required="" type="hidden" value="" />
                        <p id="member-wpayid"></p>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label">Merchant WPayID</label>
                    <div class="col-sm-6">
                        <input class="form-control" id="merchantWPayId" name="merchantWPayId" required="" type="hidden" value="" />
                        <p id="merchant-wpayid"></p>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-sm-3 control-label">Withdraw Amount</label>
                    <div class="col-sm-6">
                        <p id="withdraw-amount"></p>
                    </div>
                </div>                
                <div class="form-group">
                    <label class="col-sm-3 control-label text-right"></label>
                    <div class="col-sm-6">
                        <button type="submit" class="btn btn-primary" name="submitButton" value="Confirm">Confirm</button>                        
                        <a href="/home/index" class="btn btn-secondary" name="cancelButton" value="Cancel">Cancel</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
</form>
<script type="text/javascript">
    const validationMessage = document.getElementById("validation-message");
    validationMessage.hidden = true;
    const qrcode1 = window.qrcode;
    const video = document.createElement("video");
    const canvasElement = document.getElementById("qr-canvas");
    const canvas = canvasElement.getContext("2d");
    const btnScanQR = document.getElementById("btn-scan-qr");
    const btnCloseQR = document.getElementById("btn-scan-qr-close");

    const panelTitle = document.getElementById("panel-title");

    // A video's MediaStream object is available through its srcObject attribute
    const mediaStream = video.srcObject;
    // Through the MediaStream, you can get the MediaStreamTracks with getTracks():
    //const tracks = mediaStream.getTracks();
    var videoStream = '';
    let scanning = false;
    qrcode1.callback = res => {
        if (res) {
            //outputData.innerText = res;
            scanning = false;

            video.srcObject.getTracks().forEach(track => {
                track.stop();
            });


            console.log(res);
            var slashIndex = res.indexOf("/", 15);
            var key = res.substring(slashIndex + 1);
            console.log("slashIndex=" + slashIndex + ", key=" + key);
            var url = "https://vo2.tech/api/ShortenedUrls/" + key;
            console.log("url=" + url);

            $.ajax({
                url: url,
                headers: {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json'
                },
                type: "GET",
                contentType: 'application/json; charset=utf-8',
                success: function (result) {
                    console.log("Start Get URL by key - Success");
                    var shortUrl = result.shortUrl;
                    var longUrl = result.longUrl;

                    console.log("shortUrl - " + shortUrl);
                    console.log("longUrl - " + longUrl);
                    var m = longUrl.match(/^https:\/\/[^/]+/);
                    var hostName = m ? m[0] : null;
                    console.log("hostName - " + hostName);
                    //var paymentIdSlashIndex = longUrl.indexOf("=", 53);
                    var searchString = "receiveId=";
                    var receiveIdIndex = longUrl.indexOf(searchString);
                    var receiveId = longUrl.substring(receiveIdIndex + searchString.length);
                    console.log("receiveIdIndex:" + receiveIdIndex + ", receiveId: " + receiveId);
                    var merchantDetailUrl = "/Withdraw/Merchant/" + receiveId;
                    console.log("merchantDetailUrl:" + merchantDetailUrl);

                    //window.location.replace(longUrl);

                    // Retrieve merchant detail
                    console.log("Get merchant detail - Start");
                    $.ajax({
                        url: merchantDetailUrl,
                        headers: {
                            'Accept': 'application/json',
                            'Content-Type': 'application/json'
                        },
                        type: "POST",
                        contentType: 'application/json; charset=utf-8',
                        success: function (result) {
                            console.log("Get merchant detail - Success");
                            console.log("result - " + JSON.stringify(result));
                            if (!result.isSuccess) {
                                //const validationMessage = document.getElementById("validation-message");
                                validationMessage.hidden = false;
                                validationMessage.innerHTML = '<p style="color: red;">' +result.message +'</p>';
                                return
                            }

                            const merchantDetails = document.getElementById("merchant-details");
                            const merchantWPayId = document.getElementById("merchant-wpay-id");
                            const currency = document.getElementById("currency");
                            const merchantAlias = document.getElementById("merchantAlias");
                            const amount = document.getElementById("amount");
                            const currencyCode = document.getElementById("currencyCode");
                            merchantDetails.hidden = false;
                            console.log("MerchantAlias - " + result.merchantAlias);
                            merchantWPayId.innerText = result.merchantAlias;
                            //merchantAlias.text = result.merchantAlias;
                            $("#merchantAlias").val(result.merchantAlias);
                            console.log("amount - " + result.amount);
                            $("#amount").val(result.amount);
                            console.log("currency - " + result.currencyCode);
                            currency.innerText = result.currencyCode;
                            //currencyCode.value = result.currencyCode
                            $("#currencyCode").val(result.currencyCode);
                            panelTitle.innerHTML = '@GlobalRes.WithdrawIndexPage_Request_Subtitle';

                        },
                        error: function (jqXhr, textStatus, errorThrown) {
                            console.log("Get merchant detail - Failed");
                            console.log(jqXhr);
                            console.log(textStatus);
                            console.log(errorThrown);
                        }
                    });
                },
                error: function (jqXhr, textStatus, errorThrown) {
                    console.log("Start ShortenedUrls - Failed");
                    console.log(jqXhr);
                    console.log(textStatus);
                    console.log(errorThrown);
                }
            });

            canvasElement.hidden = true;
            btnScanQR.hidden = false;
            btnCloseQR.hidden = true;
        }
    };

    btnScanQR.onclick = () => {
        validationMessage.hidden = true;
        navigator.mediaDevices
            .getUserMedia({ video: { facingMode: "environment" } })
            .then(function (stream) {
                videoStream = stream;
                scanning = true;
                btnScanQR.hidden = true;
                canvasElement.hidden = false;
                video.setAttribute("playsinline", true); // required to tell iOS safari we don't want fullscreen
                video.srcObject = stream;
                video.play();
                tick();
                scan();
                btnCloseQR.hidden = false;
            });
    };


    btnCloseQR.onclick = () => {
        videoStream.getTracks().forEach((track) => {
            track.stop();
        });
        canvasElement.hidden = true;
        btnScanQR.hidden = false;
        btnCloseQR.hidden = true;
    };

    function tick() {
        canvasElement.height = video.videoHeight / 2;
        canvasElement.width = video.videoWidth / 2;
        canvas.drawImage(video, 0, 0, canvasElement.width, canvasElement.height);

        scanning && requestAnimationFrame(tick);
    }

    function scan() {
        try {
            qrcode.decode();
        } catch (e) {
            setTimeout(scan, 300);
        }
    }

    //function processForm(e) {
    //    if (e != null && e.preventDefault)
    //        e.preventDefault();
    //    $("#notificatorCcy").empty();
    //    $('#selectButton').removeClass("error_btn");
    //    var ccy = $('#CurrencyCode').val();
    //    if (!ccy) {
    //        $("#notificatorCcy").append('<span> Please select currency </span>');
    //        $('#selectButton').addClass("error_btn");
    //        return false;
    //    }
    //    $("form").submit();
    //    return true;
    //}

    //var form = document.getElementById('form');
    //if (form.attachEvent) {
    //    form.attachEvent("submit", processForm);
    //} else {
    //    form.addEventListener("submit", processForm);
    //}


    //$("#toIdtB").change(function () {
    //    if ($(this).val().trim().length !== 0) {
    //        $("#ToCustomer").val($(this).val());
    //        $("#ddlPriorUsedAliases").attr("disabled", true);
    //    }
    //    else {
    //        $("#ddlPriorUsedAliases").attr("disabled", false);
    //    }
    //});
    //$("#ddlPriorUsedAliases").change(function () {
    //    if ($(this).val().length !== 0) {
    //        $("#ToCustomer").val($(this).val());
    //        $("#toIdtB").val($(this).val());
    //        $("#toIdtB").prop("readonly", true);
    //    }
    //    else {
    //        $("#toIdtB").val($(this).val());
    //        $("#toIdtB").prop("readonly", false);
    //    }
    //});
</script>


<script type="text/javascript" language="javascript">
    $('#savePaymentBtn').click(function() {
        $("#Action").val("save");
    });

    $('#postPaymentBtn').click(function() {
        $("#Action").val("post");
    });

    function sublitFrom() {
        if ($('#form').valid()) {
            $('#form').submit();
            blockWindow();
        }
    }


    $(document).ready(function() {
        const panelTitle = document.getElementById("panel-title");

        // Function to validate the amount
        function validateAmount(amount) {
            var regex = /^\d+(\.\d{1,2})?$/;
            return regex.test(amount);
        }

        // Function to show/hide the validation message
        function toggleValidationMessage(valid) {
            var $errorElement = $('#amountError');
            if (valid) {
                $errorElement.hide();
            } else {
                $errorElement.text('Invalid amount! Please enter a valid amount.').show();
            }
        }

        // Amount input change event listener
        $('#amount').on('input', function () {
            var amount = $(this).val();
            var isValid = validateAmount(amount);
            toggleValidationMessage(isValid);
        });

        $('form').submit(function (e) {
            e.preventDefault(); // Prevent the form from submitting

            console.log("Form submit");
            //const merchantAlias = document.getElementById("merchantAlias");
            //const currencyCode = document.getElementById("currencyCode");
            //console.log("merchantAlias: " + merchantAlias);
            //console.log("currencyCode: " + currencyCode);


            // Create FormData object
            var formData = new FormData();
            var merchantAlias = $("#merchantAlias").val();
            var customerAlias = $("#ddlAlias").val();
            var currencyCode = $("#currencyCode").val();
            var amount = $("#amount").val();

            console.log("merchantAlias:" + merchantAlias + ", customerAlias = " + customerAlias + ", currencyCode:" + currencyCode + ", amount:" + amount);
            var isValid = validateAmount(amount);
            toggleValidationMessage(isValid);
            if (!isValid) {
                return;
            }
            //if (amount <= 0) {
            //    $('#amountError').text('Invalid amount! Please enter a valid amount.').show(); // Show the validation message
            //    return;
            //}
                
            // Retrieve embedded form verification token
            var token = $('input[name=__RequestVerificationToken]').val();
            formData.set("__RequestVerificationToken", token);
            // Added data to form data
            formData.set("MerchantAlias", merchantAlias);
            formData.set("CustomerAlias", customerAlias);
            formData.set("CurrencyCode", currencyCode);
            formData.set("Amount", amount);

            var submitButtonValue = $('button[type="submit"][clicked=true]').val(); // Get the value of the clicked submit button
            console.log("submitButtonValue: " + submitButtonValue);

            if (submitButtonValue === 'Request') {
                console.log("Request withdraw");
                $.ajax({
                    url: '@Url.Action("Create", "Withdraw")',
                    type: 'POST',
                    data: formData,
                    processData: false,
                    contentType: false,
                    success: function (result) {
                        // Handle the success response
                        console.log("Request withdraw-Success, result:" + JSON.stringify(result));
                        if (result.isSuccess && result.paymentId != "00000000-0000-0000-0000-000000000000") {
                            $("#paymentId").val(result.paymentId);
                            const merchantDetails = document.getElementById("merchant-details");
                            const withdrawConfirmation = document.getElementById("withdraw-confirmation");
                            const paymentReference = document.getElementById("payment-reference");
                            const memberWpayid = document.getElementById("member-wpayid");
                            const merchantWpayid = document.getElementById("merchant-wpayid");
                            const withdrawAmount = document.getElementById("withdraw-amount");
                            //const merchantWPayId = document.getElementById("merchant-wpay-id");
                            //const currency = document.getElementById("currency");
                            //const merchantAlias = document.getElementById("merchantAlias");
                            //const currencyCode = document.getElementById("currencyCode");
                            panelTitle.innerHTML = '@GlobalRes.WithdrawIndexPage_Confirmation_Subtitle';
                            paymentReference.innerText = result.paymentReference;
                            memberWpayid.innerText = customerAlias;
                            merchantWpayid.innerText = merchantAlias;
                            withdrawAmount.innerText = amount + " " + currencyCode;
                            withdrawConfirmation.hidden = false;
                            merchantDetails.hidden = true;
                        }
                    },
                    error: function (xhr, textStatus, errorThrown) {
                        // Handle the error response
                        console.log("Request withdraw-Error");
                        console.log(xhr);
                        console.log(textStatus);
                        console.log(errorThrown);

                    }
                });
            } else if (submitButtonValue === 'Confirm') {
                // Handle confirm withdraw
                console.log("Confirm withdraw");
                // Create FormData object
                var confirmFormData = new FormData();
                var paymentId = $("#paymentId").val();
                // Retrieve embedded form verification token
                var token = $('input[name=__RequestVerificationToken]').val();
                confirmFormData.set("__RequestVerificationToken", token);
                // Added data to form data
                confirmFormData.set("PaymentId", paymentId);

                $.ajax({
                    url: "@Url.Action("Confirm", "Withdraw")",
                    type: "POST",
                    data: confirmFormData,
                    processData: false,
                    contentType: false,
                    success: function (result, textStatus, jqXHR) {
                        //result - response from server
                        console.log("Confirm withdraw - is success: " + result.isSuccess);

                        if (result.isSuccess == true) {
                            //window.url('/Payment/Details/' + paymentId);
                            window.location.replace('/Payment/Details/' + paymentId);
                        }
                        else {

                        }

                    },
                    error: function (jqXhr, textStatus, errorThrown) {
                        console.log("Confirm withdraw - Failed");
                        console.log(jqXhr);
                        console.log(textStatus);
                        console.log(errorThrown);
                    }
                });
            }
        });

        // Set the clicked attribute when a submit button is clicked
        $('button[type="submit"]').click(function () {
            $('button[type="submit"]').removeAttr('clicked');
            $(this).attr('clicked', 'true');
        });

    });
    function showQRIntro() {
        return confirm("Use your camera to take a picture of a QR code.");
    }

    $("#mobscanqr").click(function () {
        $('.qrcode-text-btn').click();
    });

    function openQRCamera(node) {
        var reader = new FileReader();
        reader.onload = function () {
            node.value = "";
            qrcode.callback = function (res) {
                if (res instanceof Error) {
                    alert("No QR code found. Please make sure the QR code is within the camera's frame and try again.");

                } else {
                    console.log(res);
                    var slashIndex = res.indexOf("/", 16);
                    var key = res.substring(slashIndex + 1);
                    console.log("slashIndex=" + slashIndex + ", key=" + key);
                    var url = "https://vo2.tech/api/ShortenedUrls/" + key;
                    console.log("url=" + url);

                    $.ajax({
                        url: url,
                        headers: {
                            'Accept': 'application/json',
                            'Content-Type': 'application/json'
                        },
                        type: "GET",
                        contentType: 'application/json; charset=utf-8',
                        success: function (result) {
                            console.log("Start Get URL by key - Success");
                            var shortUrl = result.shortUrl;
                            var longUrl = result.longUrl;

                            console.log("shortUrl - " + shortUrl);
                            console.log("longUrl - " + longUrl);
                            var m = longUrl.match(/^https:\/\/[^/]+/);
                            var hostName = m ? m[0] : null;
                            console.log("hostName - " + hostName);
                            var paymentIdSlashIndex = longUrl.indexOf("=", 53);
                            var receiveId = longUrl.substring(paymentIdSlashIndex + 1);
                            console.log("paymentIdSlashIndex=" + paymentIdSlashIndex + ", receiveId=" + receiveId);
                             window.location.replace(longUrl);
                        },
                        error: function (jqXhr, textStatus, errorThrown) {
                            console.log("Start ShortenedUrls - Failed");
                            console.log(jqXhr);
                            console.log(textStatus);
                            console.log(errorThrown);
                        }
                    });
                }
            };
            console.log(reader.result);
            qrcode.decode(reader.result);
        };
        reader.readAsDataURL(node.files[0]);
    }

    $('#selectButton').click(function () {
        setTimeout(function () { $('#searchabletext').focus(); }, 100);
    });
    $('#ddlmenu li a').on('click',
        function () {
            console.log("Func work");
            var id = $(this).data('pdsa-dropdown-val');
            var currencyText = $(this).data('pdsa-dropdown-text');
            var id1 = "";

            if (id != null) {
                console.log(id);
                var buttons = document.getElementById("selectButton");

                try {
                    id1 = id.toLowerCase();
                } catch (e) {
                    id1 = id;
                }

                buttons.innerHTML = '<img src="/Content/assets/images/onebyone.png" class="flag flag-' + id1 + '"/>' + '  ' +
                    currencyText +
                    '&nbsp;<span class="caret" style="position: absolute; top: 45%; left: 97.5%; color: #555555 !important; border-top: 6px dashed !important; border-right: 3px solid transparent !important;border-left: 3px solid transparent !important"></span>';
                $("#ddlmenu").val(id);
                console.log("#ddlmenu");
                $("#CurrencyCode").val(id);
                $("#searchabletext").val("");
                $("#ddlmenu li").show();
                console.log("#CurrencyCode");
                $('#selectButton').removeClass("error_btn");
                $("#notificatorCcy").empty();
            }
            else {
                var buttons = document.getElementById("selectButton");
                buttons.innerHTML = '@String.Format(GlobalRes.Shared_NewInstantPaymentFormPage_SelectCurrency)' + '&nbsp;<span class="caret" style="position: absolute; top: 45%; left: 97.5%; color: #555555 !important; border-top: 6px dashed !important; border-right: 3px solid transparent !important;border-left: 3px solid transparent !important"></span>';
                $("#ddlmenu").val(null);
                //console.log("#ddlmenu");
                $("#CurrencyCode").val(null);
                $("#searchabletext").val("");
                $("#ddlmenu li").show();
                console.log("#CurrencyCode");
            }
        });
    $("#searchabletext").on("input", function () {
        var selText = $(this).val();
        console.log("selText: " + selText);

        $("#ddlmenu li").hide();
        $("#searchabli").show();
        $("#ddlmenu li").each(function () {
            var thisVal = $(this).data('pdsa-dropdown-val');
            var thisText = $(this).text().trim();
            var thisTextArray = thisText.split(" ");
            var currencyCode = thisTextArray[1];
            var index = thisText.trim().indexOf(selText.toUpperCase())
            if (thisText.toUpperCase().indexOf(selText.toUpperCase()) >= 0) {
                $(this).show();
            }
        });
        $("#searchabletext").one("keydown", function (e) {
            if (e.which == 13 || e.which == 10) {
                if ($("#ddlmenu li:visible").first() !== null) {
                    var el = $("#ddlmenu li:visible a").attr('display', 'list-item').first();
                    var id = el.data('pdsa-dropdown-val');
                    var currencyText = el.data('pdsa-dropdown-text');
                    if (id != null) {
                        console.log("Entered");

                        console.log(id);
                        var buttons = document.getElementById("selectButton");
                        buttons.innerHTML = '<img src="/Content/assets/images/onebyone.png" class="flag flag-' + id.toLowerCase() + '"/>' + '  ' +
                            id +
                            '&nbsp;<span class="caret" style="position: absolute; top: 45%; left: 97.5%; color: #555555 !important; border-top: 6px dashed !important; border-right: 3px solid transparent !important;border-left: 3px solid transparent !important"></span>';
                        $("ddlmenu").val(id);
                        console.log("#ddlmenu");
                        $("#CurrencyCode").val(id);
                        $("#searchabletext").val("");
                        $("#ddlmenu li").show();
                        $('#selectButton').removeClass("error_btn");
                        $("#notificatorCcy").empty();
                    }

                    else { console.log("Not Entered"); }
                }
            }
        });

    });
</script>